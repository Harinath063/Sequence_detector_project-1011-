# Sequence_detector_project-1011-
This project implements a 1011 sequence detector using Verilog HDL. The design detects the binary sequence "1011" from a serial input stream and asserts the output when the sequence is found. It is based on a finite state machine (FSM) and can be designed using either Mealy or Moore models.
